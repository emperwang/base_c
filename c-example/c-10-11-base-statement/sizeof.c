#include <stdio.h>

int main(int argc, char* argv[]){
	
	printf("the int's char is %d\n", sizeof(int));
	printf("the long;s char is %d\n", sizeof(long));

	printf("the short's char is %d\n", sizeof(short));
	printf("the double's char is %d\n", sizeof(double));
	printf("the float's char is %d\n", sizeof(float));

	return 0;
}
